name: Deploy (Cloudflare Pages)

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  workflow_dispatch:

permissions:
  contents: read
  deployments: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist

  deploy:
    name: Deploy to Cloudflare Pages
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Debug — check secret presence
        env:
          TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          ACCT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PROJ: ${{ secrets.CLOUDFLARE_PAGES_PROJECT }}
        run: |
          if [ -z "$TOKEN" ]; then echo "CLOUDFLARE_API_TOKEN is EMPTY"; else echo "CLOUDFLARE_API_TOKEN is set (length: ${#TOKEN})"; fi
          if [ -z "$ACCT" ]; then echo "CLOUDFLARE_ACCOUNT_ID is EMPTY"; else echo "CLOUDFLARE_ACCOUNT_ID is set (length: ${#ACCT}, value: $ACCT)"; fi
          if [ -z "$PROJ" ]; then echo "CLOUDFLARE_PAGES_PROJECT is EMPTY"; else echo "CLOUDFLARE_PAGES_PROJECT is set: $PROJ"; fi

      - name: Debug — verify Cloudflare credentials
        env:
          TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          ACCT: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PROJ: ${{ secrets.CLOUDFLARE_PAGES_PROJECT }}
        run: |
          echo "=== Token verify ==="
          curl -sS -o /tmp/verify.json -w "HTTP %{http_code}\n" \
            -H "Authorization: Bearer $TOKEN" \
            https://api.cloudflare.com/client/v4/user/tokens/verify
          cat /tmp/verify.json | head -c 400; echo
          echo "=== Pages project lookup ==="
          curl -sS -o /tmp/proj.json -w "HTTP %{http_code}\n" \
            -H "Authorization: Bearer $TOKEN" \
            "https://api.cloudflare.com/client/v4/accounts/$ACCT/pages/projects/$PROJ"
          cat /tmp/proj.json | head -c 400; echo

      - name: Publish to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{ secrets.CLOUDFLARE_PAGES_PROJECT }}
          directory: dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.event_name == 'pull_request' && github.head_ref || 'main' }}
